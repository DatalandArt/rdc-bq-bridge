# Sample Prometheus scrape config for the RDC-BigQuery bridge.
# Default bridge bind is 127.0.0.1:9090; run Prometheus on the same host.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: rdc-bridge
    static_configs:
      - targets: ['127.0.0.1:9090']
